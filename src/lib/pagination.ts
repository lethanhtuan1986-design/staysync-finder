/**
 * Default pageSize cho danh sách phòng/đánh giá. Sử dụng ở các nơi cần
 * pageSize "chuẩn" (SearchPage, SavedRooms, Index, CustomerReviews,...).
 * Một số chỗ có nhu cầu đặc thù (map markers, catalog dropdown, similar rooms,
 * property reviews) vẫn giữ giá trị riêng.
 */
export const PAGE_SIZE_DEFAULT = 30;
